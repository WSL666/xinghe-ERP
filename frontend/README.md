# frontend · 着陆页

原生 HTML + CSS + JavaScript 实现的落地页（landing page）。

## 文件

- `index.html`
- `styles.css`
- `script.js`

## 使用

由主应用 `backend/` 的 FastAPI `StaticFiles` 托管，通过 `https://wangshilin888.com:8443` 访问。

页面使用远程视频、Google Fonts 和 svgl.app 图标，需要联网才能完整显示。
